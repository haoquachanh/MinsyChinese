// import { useState } from 'react';
import { Icon } from "../Icons";

type Personal = {
  topic: number;
  question: string;
  answer: string;
};
export default function Personal() {
  // const [topic, setTopic] = useState('1');
  return <div className="flex flex-col w-[80%] overflow-auto"></div>;
}
